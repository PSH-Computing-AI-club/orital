import Layout from "~/components/controlpanel/layout";

import {Route} from "./+types/admin_.events.create";

export default function AdminEventsCreate(_props: Route.ComponentProps) {
    return <Layout.Container>Events create unda construction</Layout.Container>;
}
