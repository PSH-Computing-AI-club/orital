import Layout from "~/components/controlpanel/layout";

import {Route} from "./+types/admin_.events";

export default function AdminEvents(_props: Route.ComponentProps) {
    return <Layout.Container>Events index unda construction</Layout.Container>;
}
