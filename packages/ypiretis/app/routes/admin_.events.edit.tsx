import Layout from "~/components/controlpanel/layout";

import {Route} from "./+types/admin_.events.edit";

export default function AdminEventsEdit(_props: Route.ComponentProps) {
    return <Layout.Container>Events edit unda construction</Layout.Container>;
}
